import React, {useState, useRef, useEffect} from "react";
import { DrugChart } from "./assets/react_components/ChartComponents.jsx"
export default function StatisticsPage(match) {

  return (
    <main>
        <DrugChart></DrugChart>
    </main>
  );
}
